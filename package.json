{
  "name": "michaelnease.com",
  "description": "Michael Nease: Portfolio",
  "version": "1.0.0",
  "scripts": {
    "heroku-prebuild": "npm install --prefix ./client && npm run build --prefix ./client",
    "heroku-postbuild": "npm install --prefix ./server && npm start --prefix ./server"
  },
  "engines": {
    "node": "11.14.0",
    "npm": "6.7.0"
  }
}
